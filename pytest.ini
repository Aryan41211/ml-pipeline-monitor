[pytest]
pythonpath = src
addopts =
    --cov=ml_pipeline_monitor
    --cov-config=.coveragerc
    --cov-report=term-missing
    --cov-report=html
    --cov-fail-under=80
    --ignore=tests/load
testpaths =
    tests